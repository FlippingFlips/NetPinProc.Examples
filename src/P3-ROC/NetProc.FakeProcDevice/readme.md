# NetPinProc.FakeProcDevice
---

Tests running a `FakeProcdevice`, simulated. For testing the main loop Loop to `GetEvents` from device.

Type switch numbers in console window. 32 = trough_1 , 33 = trough_2